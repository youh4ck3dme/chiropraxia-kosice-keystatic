---

---

<div
  id="cookie-banner"
  class="pointer-events-none fixed bottom-6 left-6 z-50 translate-y-[200%] transform opacity-0 transition-all duration-700 ease-in-out"
>
  <div class="glass-card flex max-w-[320px] flex-col gap-4 border-white/10 p-4 shadow-2xl">
    <div class="flex-1">
      <h3 class="mb-1 flex items-center gap-2 text-sm font-bold text-white">
        <span class="text-lg">🍪</span> Sušienky? (Cookies)
      </h3>
      <p class="text-chrome-gray text-[11px] leading-tight">
        Používame ich na analýzu návštevnosti, aby sme vedeli, či naše SEO funguje. Žiadne
        špehovanie, len dáta o tom, koľko ľudí bolí chrbát.
      </p>
    </div>
    <div class="flex items-center gap-2">
      <button
        id="cookie-reject"
        class="text-chrome-gray flex-1 rounded-lg border border-white/5 px-3 py-1.5 text-[10px] transition-colors hover:text-white"
      >
        Odmietnuť
      </button>
<<<<<<< HEAD
      <button
        id="cookie-accept"
        class="bg-aurora shadow-aurora/20 flex-1 rounded-lg px-3 py-1.5 text-[10px] font-bold !text-black shadow-lg transition-all hover:brightness-110 active:scale-95"
      >
=======
      <button id="cookie-accept" class="flex-1 px-3 py-1.5 bg-aurora text-black! text-[10px] font-bold rounded-lg shadow-lg shadow-aurora/20 transition-all hover:brightness-110 active:scale-95">
>>>>>>> origin/main
        Súhlasím
      </button>
    </div>
  </div>
</div>

<style>
  #cookie-banner.visible {
    transform: translateY(0);
    opacity: 1;
    pointer-events: auto;
  }
</style>

<script>
  const banner = document.getElementById('cookie-banner');
  const acceptBtn = document.getElementById('cookie-accept');
  const rejectBtn = document.getElementById('cookie-reject');

  const CONSENT_KEY = 'chiro_cookie_consent';

  // Check if user has already made a choice
  const hasConsent = localStorage.getItem(CONSENT_KEY);

  if (!hasConsent) {
    // Show banner after a small delay
    setTimeout(() => {
      banner?.classList.add('visible');
    }, 1000);
  } else if (hasConsent === 'granted') {
    // Initialize analytics if already granted
    enableAnalytics();
  }

  acceptBtn?.addEventListener('click', () => {
    localStorage.setItem(CONSENT_KEY, 'granted');
    banner?.classList.remove('visible');
    enableAnalytics();
  });

  rejectBtn?.addEventListener('click', () => {
    localStorage.setItem(CONSENT_KEY, 'denied');
    banner?.classList.remove('visible');
  });

  function enableAnalytics() {
    // Defines the gtag function if not already defined (usually done in Layout head)
    // and pushes the consent update.
    // Real GA initialization happens in Layout.astro based on this or immediately if configured
    if (typeof (window as any).gtag === 'function') {
      (window as any).gtag('consent', 'update', {
        ad_storage: 'granted',
        analytics_storage: 'granted',
      });
    }
  }
</script>
